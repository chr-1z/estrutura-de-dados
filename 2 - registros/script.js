// declaração do tipo de dado

var estrutura_livro = { 
    nome: "As aventuras de Tintim",
    preco: "20.00",
    pagina: 100
}

// declaração das variáveis

console.log(`O nome do livro é: ${estrutura_livro.nome}, possui ${estrutura_livro.pagina} páginas e custa R$${estrutura_livro.preco}`);
